# Convex Functions

This directory contains the Convex backend for the web app.

Run `pnpm --filter @lfas/web exec convex dev` after configuring a Convex
deployment to regenerate `convex/_generated` and push functions.
